/**
 * @todo Create string resource file
 * @body String resource file with constants for string texts and labels would help to make the app
 * more consistent and eliminate typo errors.
 */
export const resource = {
  // Flesh out resources
};
